import * as React from 'react';
import {
    Page,
    Carousel,
} from '../components/common';

const Home = (props) => (
    <Page title="Home">
        <Carousel />
        Homepage
    </Page>
);

export default Home;
